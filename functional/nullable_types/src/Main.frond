// 功能验证：类型后缀链 —— T?[]（元素级 nullable）与后缀组合
// 此前 `T?[]` 因 `?` 后缀循环不消费 `[` 而 parse error。现统一为交错后缀链
//（? / [] / [N]，从左到右）：i32?[] = 元素可空的数组；i32[]? = 可空数组。
// 字面写出的 T?? 被 sema 拒绝（nullable 无 Some 构造器,嵌套无意义,静默折叠会
// 让 Kotlin/TS 用户带着 Some(null) 的错误预期写代码）——但类型机器内部仍折叠
// 泛型实例化产生的嵌套（`T?` 当 T := X?）,保证泛型组合闭合。见负向
// double_nullable_*.frond。
var failures: i32 = 0

fun check(cond: bool, name: str): void {
    if cond { println("  PASS: {name}") }
    else { failures = failures + 1; println("  FAIL: {name}") }
}
type NU = NU(name: str, age: i32?)

fun nv_call(): bool {
    fire_and_forget()
    true
}
fun fire_and_forget(): void {
    noop_throwing()
}
fun noop_throwing(): Throw<void, str> {
    Ok(void)
}

fun summary(): void {
    println("")
    if failures == 0 { println("RESULT: ALL PASSED") }
    else { println("RESULT: {failures} FAILED") }
}

// ── 泛型闭合：`T?` 当 T 实例化为 nullable 时内部折叠为 T? ──
fun firstOr<T>(arr: T[]): T? {
    if arr.len() == 0 {
        null
    } else {
        arr[0]
    }
}
type Alias = i32?

// ── T?[]：元素级 nullable ──
fun makeElems(): i32?[] {
    val a: i32?[] = [1, null, 3]
    a
}
fun countNonNull(a: i32?[]): i32 {
    var n: i32 = 0
    var i: usize = 0
    while i < a.len() {
        if a[i] != null {
            n = n + a[i]
        }
        i = i + 1
    }
    n
}
fun elemOr(a: i32?[], d: i32): i32 { a[0] ?? d }

fun main(): void {
    // cast 位置
    val seven: i32 = 7
    check(seven as i32? != null, "cast as i32?")
    check(seven as i32?[] == null || true, "cast as i32?[] parses")

    // T?[] 字面量与读写
    val arr: i32?[] = [1, null, 3]
    check(arr.len() == 3, "T?[] len")
    check(arr[0] == 1 && arr[2] == 3, "T?[] non-null elems")
    check(arr[1] == null, "T?[] null elem read")
    var m: i32?[] = [1, 2]
    m[0] = null
    check(m[0] == null && m[1] == 2, "T?[] write null")

    // null 开头 / 全 null 字面量
    val lead = [null, 1, null, 4]
    check(lead[0] == null && lead[3] == 4, "null-leading literal")
    val alln: i32?[] = [null, null]
    check(alln[0] == null && alln[1] == null, "all-null literal")

    // 泛型闭合：T := i32? 时 (i32?)? 内部折叠为 i32?
    val g1 = firstOr([7, 8])
    check(g1 == 7, "generic T=i32 first")
    val g2 = firstOr(arr)
    check(g2 == 1, "generic T=i32? first (collapsed)")
    val g3 = firstOr(alln)
    check(g3 == null, "generic T=i32? null elem")
    // Alias? ：命名类型解析为 nullable 后再包 ? —— 合法且折叠
    val al: Alias? = null
    check(al == null, "Alias? (resolved nesting) ok")

    // 函数签名
    val r = makeElems()
    check(r[1] == null && r.len() == 3, "fn returns T?[]")
    check(countNonNull([1, null, 3, null, 5]) == 9, "indexed iteration sum")
    check(elemOr([null, 2], 100) == 100, "elvis on null elem")
    check(elemOr([9, 2], 100) == 9, "elvis on value elem")

    // 组合后缀
    val na: i32?[]? = null
    check(na == null, "i32?[]? null")
    val na2: i32?[]? = [1, null]
    check(na2 != null, "i32?[]? present")
    val nested: i32?[][] = [[1, null], [null, 2]]
    check(nested[0][1] == null && nested[1][0] == null, "nested T?[][]")
    check(nested[1][1] == 2, "nested value")
    val sized: i32?[3] = [1, null, 3]
    check(sized[1] == null && sized.len() == 3, "sized T?[3]")

    // 流窄化：null 检查后可直接当非空值用
    val e: i32? = arr[0]
    if e != null {
        check(e + 1 == 2, "flow-narrowed elem arithmetic")
    } else {
        check(false, "flow-narrowed elem arithmetic (wrong branch)")
    }

    // ── nullable 相等比较(修复前 tag 严格比较静默 false)────────
    val a: i64? = 42
    val n: i64? = null
    check(a == 42, "i64? == bare i32 literal (value equal)")
    check(a != 43, "i64? != literal")
    check(!(a == 43), "i64? == wrong literal is false")
    check(n == null && a != null, "null equality")
    check(n != 42, "null != value")
    val f: f64? = 2.5
    check(f == 2.5, "f64? == literal")
    val f32v: f32? = 2.5
    check(f32v == 2.5, "f32? == f64 literal (cross-width value equal)")
    val s2: str? = "hi"
    check(s2 == "hi", "str? == str literal")

    // ── 字面量直标 nullable(修复前报 annotation mismatch)──────
    val lit: i64? = 42
    check(lit == 42, "val a: i64? = 42 direct literal annotation")
    val biglit: u64? = 18446744073709551615
    check(biglit != null, "u64? = max literal annotation")

    // ── 解包泄漏守卫的正向保护(负向见 nullable_leak_*.frond)────
    // 字段流窄化:`if u.age != null` 内 u.age 按 i32 使用(修复前窄化 fact
    // 因 env 查不到点路径而从不建立,靠 T?→T 静默放行掩盖)。
    val nu = NU("alice", 30)
    if nu.age != null {
        val inner: i32 = nu.age
        check(inner + 1 == 31, "field flow-narrow binds i32")
    } else {
        check(false, "field flow-narrow (wrong branch)")
    }
    // match 绑定臂窄化:null 臂之后的绑定变量收窄为 inner(std Path 惯用)。
    val ms: str? = "hello"
    val mlen = match ms {
        null => 0
        s => s.len()
    }
    check(mlen == 5, "match binding arm narrows to inner")
    // Throw<void, E> 尾调用进 void 函数是惯用模式(无值载荷,不算泄漏)。
    check(nv_call(), "Throw<void,_> tail into void fn stays legal")

    summary()
}
