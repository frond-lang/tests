// [INTENTIONAL-FAIL] throw 表达式参与运算(never 与 i32)
// 故意触发 bug/限制，sema/parser 报错是预期行为，非回归
fun f(): Throw<i32, Error> {
    val x: i32 = throw Error("x")
    Ok(x)
}
fun main(): void {
    println("ok")
}
