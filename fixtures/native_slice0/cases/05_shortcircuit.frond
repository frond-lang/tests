// EXPECT-EXIT 1
// 短路语义(对齐引擎 Bug#38):x=0 时 && 的 RHS 除零不得求值——非短路
// 实现在原生侧是 sdiv 0 = x86 #DE 崩溃;|| 左真直达。覆盖 &&、||、!、
// 比较链、bool 返回(zext → 退出码 0/1)。
fun main(): bool {
    val x = 0
    val a = x != 0 && 10 / x > 0
    val b = x == 0 || 10 / x > 0
    b && !a
}
