pub type P = P(pub x: i64, pub y: i64)
pub type Color = | Red | Green | Blue

pub fun dbl(n: i64): i64 { n * 2 }
pub fun pick(a: i64, b: i64): i64 { if a > b { a } else { b } }
pub fun sum_p(p: P): i64 { p.x + p.y }
